[
  {demo, []},
  {kernel,
  [{logger,
    [{handler, default, logger_std_h,
      #{ config => #{ file => "log/demo.log",
                      max_no_bytes => 4096,
                      max_no_files => 5},
         formatter => {logger_formatter, #{}}}}]}]}
].
